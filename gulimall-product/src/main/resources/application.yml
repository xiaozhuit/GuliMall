spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://47.109.86.147:3306/gulimall-pms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

# MapperScan
# sql映射文件位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:   # 配置每个表的主键自增长
    db-config:
      id-type: auto

server:
  port: 10000
